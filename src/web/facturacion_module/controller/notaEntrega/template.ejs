<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Nota de Entrega</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: "Courier New", Courier, monospace;
            color: black !important;
            font-size: 12px;
            padding: 10px;
            margin: 0;
        }

        *,
        *::before,
        *::after {
            color: black !important;
            border-color: black !important;
        }

        .qr-code {
            width: 100px;
            height: 100px;
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 10px;
        }

        .header-left {
            width: 50%;
            text-align: left;
        }

        .header-right {
            width: 50%;
            text-align: right;
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }

        .header-top {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0px;
            margin-bottom: 5px;
        }

        .title-container {
            flex-grow: 1;
            text-align: center;
            padding: 0px;
            margin-bottom: 5px;
        }

        .qr-container {
            width: 100px;
            text-align: right;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10px;
            margin-bottom: 20px;
            margin-top: 20px;
        }

        table,
        th,
        td {
            border: 1px solid black !important;
        }

        th,
        td {
            padding: 5px;
            text-align: center;
            padding: 3px;
        }

        .header-container,
        .header-top,
        .table {
            margin-bottom: 5px !important;
        }

        .header-left p,
        .header-right p {
            margin: 2px 0 !important;
        }

        th {
            background-color: #f4f4f4;
            font-weight: bold;
            padding: 3px;
        }

        th:nth-child(1),
        td:nth-child(1) {
            width: 10%;
            min-width: 100px;
            text-align: left;
        }

        tbody tr:nth-child(odd) {
            background-color: #f9f9f9;
        }

        tbody tr:nth-child(even) {
            background-color: #ffffff;
        }

        .footer-text {
            margin-top: 15px;
            padding: 0px;
            text-align: right;
            font-style: italic;
        }

        .footer-signatures {
            margin-top: 55px;
            display: flex;
            justify-content: space-around;
            text-align: center;
        }

        .footer-signatures div {
            display: inline-block;
            width: 20%;
            border-top: 1px solid black;
            margin: 0 5px;
            margin-top: 30px;
        }

        .comments {
            margin: 1rem 0rem;
            padding: 1rem;
        }
    </style>
</head>

<body class="text-black">
    <div class="content text-black">
        <div class="header-top">
            <div class="title-container">
                <h6 class="text-center">LABORATORIOS IFA</h6>
                <h6 class="text-center">NOTA DE ENTREGA #<%= data.DocNum %></h6>
                <h6 class="text-center">ALMACEN : <%= data.WhsCode %></h6>
                <h6 class="text-center"><%= data.WhsName %></h6>
            </div>
            <div class="qr-container">
                <img class="qr-code" src="<%= qrCode %>" alt="QR Code">
            </div>
        </div>
        <hr>

        <div class="header-container">
            <div class="header-left">
                <p><strong>Usuario: <%= data.USER_CODE %></strong></p>
                <p><strong>Creado por: <%= data.U_NAME %></strong></p>
                <p><strong>Fecha de creación: <%= new Date(data.DocDate).toLocaleDateString('es-ES') %> </strong></p>
                <p><strong>Hora:<%= data.time %></strong></p>
            </div>
            <div class="header-right">
                <p><strong>Teléfono: <%= data.Phone1 %> </strong></p>
                <p><strong>Dirección: <%= data.Address2 %> </strong></p>
                <p><strong>Zona: <%= data.U_Zona %></strong></p>
                <p><strong>Cliente: <%= data.CardCode %> - <%= data.CardName %></strong></p>
            </div>
        </div>
        <hr>

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Código</th>
                    <th>Descripción</th>
                    <th>Lote</th>
                    <th>Fecha Exp.</th>
                    <th>Cant.</th>
                    <th>Precio</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <% let totalGeneral = 0; %>
                <% data.detailsList.forEach(item => {
                    totalGeneral += parseFloat(item.LineTotal); %>
                    <tr>
                        <td><strong> <%= item.ItemCode %> </strong> </td>
                        <td><strong><%= item.Dscription %></strong></td>
                        <td><strong> <%= item.BatchNum %></strong></td>
                        <td><strong><%= new Date(item.ExpDate).toLocaleDateString('es-ES') %></strong></td>
                        <td><strong><%= item.Quantity %></strong></td>
                        <td><strong><%= parseFloat(item.Price).toFixed(2) %></strong></td>
                        <td><strong><%= parseFloat(item.LineTotal).toFixed(2) %></strong></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="comments">
            <p><strong>Comentario: <%= data.Comments %></strong> </p>
            <p><strong>Vendedor: <%= data.SlpName %></strong> </p>
        </div>

        <div class="footer-text">
            <p><strong>Total General: <%= totalGeneral.toFixed(2) %> BOB</strong> </p>
            <p><strong>Condición de pago: <%= data.PymntGroup %></strong> </p>
        </div>

        <div class="footer-signatures">
            <div><strong> Creado por <%= data.U_NAME %></strong></div>
            <div><strong>Preparado por <%= data.U_N %></strong></div>
            <div><strong>Revisado por <%= data.U_N %></strong></div>
            <div><strong>Recibí conforme <%= data.U_N %></strong></div>
        </div>
    </div>
</body>

</html>
