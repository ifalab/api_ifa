<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nota del Pedido</title>
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> -->
    <style>
        body {
            font-family: "Courier New", Courier, monospace;     
            margin: 20px;
            color: #333;
        }

        h1, h2, h3, h5 {
            text-align: center;
            color: #4d4c4c;
            margin: 10px 0;
        }

        h1 {
            font-size: 28px;
        }

        h2 {
            font-size: 20px;
        }

        p {
            font-size: 14px;
            margin: 5px 0;
        }

        .container {
            max-width: 800px;
            margin: auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .header img {
            max-width: 100px;
            margin-bottom: 10px;
        }

        .header h1 {
            font-size: 24px;
            margin: 0;
        }

        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 5px;
            text-align: center;
            font-size: 14px;
        }

        th {
            background-color: #f4f4f4;
            font-weight: bold;
        }

        tbody tr:nth-child(odd) {
            background-color: #f9f9f9;
        }

        tbody tr:nth-child(even) {
            background-color: #ffffff;
        }

        /* QR Code */
        .qr-code {
            text-align: center;
            margin-top: 5px;
        }

        .qr-code img {
            width: 150px;
            height: 150px;
        }
        .content {
            margin: 20px 10px;
            /* Evita que el contenido quede pegado al borde */
        }

        .total-row {
            font-weight: bold;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #888;
        }
    </style>
</head>

<body>
    <div class="content">
        <h2 class="text-center">LABORATORIOS IFA</h2>
        <h5 class="text-center"><strong>NOTA DEL PEDIDO #<%= data.DocNum %></strong>
        </h5>
        <h5 class="text-center"><strong>ALMACEN : <%= data.WhsCode %></strong>
        </h5>
        <h5 class="text-center"><strong><%= data.WhsName %></strong>
        </h5>
        <hr>
        <div class="d-flex justify-content-between">
            <div class="">
                <p><strong>Usuario: <%= data.USER_CODE %></strong>
                </p>
                <p><strong>Creado por: <%= data.U_NAME %></strong>
                </p>
                <p><strong>Fecha de creación: <%= new Date(data.DocDate).toLocaleDateString('es-ES') %></strong>
                </p>
                <p><strong>Hora: <%= data.time %></strong>
                </p>
                <p><strong>Teléfono: <%= data.Phone1 %></strong>
                </p>
                <p><strong>Dirección: <%= data.Address2 %></strong>
                </p>
                <p><strong>Zona: <%= data.U_Zona %></strong>
                </p>
                <p><strong>Cliente: <%= data.CardCode %> - <%= data.CardName %></strong>
                </p>
                <p><strong>Condición de pago: <%= data.PymntGroup %></strong></p>
            </div>
        </div>
        <hr>
        <table class="table">
            <thead>
                <tr>
                    <th>Código</th>
                    <th>Descripción</th>
                    <th>Cant.</th>
                    <th>Precio Unit.</th>
                    <th>Desc.</th>
                    <th>SubTotal</th>
                </tr>
            </thead>
            <tbody>
                <% let totalGeneral=0; %>
                    <% data.detailsList.forEach(item=> {
                        totalGeneral += parseFloat(item.LineTotal);
                        %>
                        <tr>
                            <td>
                                <strong><%= item.ItemCode %></strong>
                                
                            </td>
                            <td>
                                <strong><%= item.Dscription %></strong>
                            </td>
                            <td>
                                <strong><%= Math.round(item.Quantity) %></strong>
                            </td>
                            <td>
                                <strong><%= parseFloat(item.Price).toFixed(2) %></strong>
                            </td>
                            <td>
                                <strong><%= parseFloat(item.DescLinea).toFixed(2) %></strong>
                            </td>
                            <td>
                                <strong><%= parseFloat(item.LineTotal).toFixed(2) %></strong>
                            </td>
                        </tr>
                        <% }); %>
            </tbody>
            <tfoot>
                <tr class="total-row">
                    <td colspan="5" class="text-end">Total General:</td>
                    <td>
                        <strong><%= totalGeneral.toFixed(2) %> BOB</strong>
                    </td>
                </tr>
            </tfoot>
        </table>
        <div class="footer-text">
            <!-- <p><strong>Empleado:</strong> MEDLING ARDAYA JENNIFER</p> -->
            
            <p><strong>Glosa: <%= data.JrnlMemo %></strong>
                
            </p>
            <!-- <p><strong>Glosa:</strong> solicitado por correo ent. hoy en el trans de la tarde basado en pedidos de cliente 240006460.</p> -->
            <!-- <p><strong>Total en texto:</strong> Eleven Thousand Four Hundred Ninety-Five And 39/100 Bolivianos</p> -->
        </div>
<br>
        <div class="qr-code">
            <!-- <p>Escanea para verificar el pedido:</p> -->
            <img src="<%= qrCode %>" alt="QR Code">
        </div>

        <!-- <div class="footer">
            
        </div> -->


    </div>
<footer class="footer">
    <p><strong>LABORATORIOS IFA - Todos los derechos reservados</strong></p>
    <p><strong>Documento generado el <%= new Date().toLocaleDateString() %></strong></p>
</footer>

</body>

</html>